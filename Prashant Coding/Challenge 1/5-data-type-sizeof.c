#include<stdio.h>
int main()
{
  int a;
  char b;
  float c;
  double d;
  printf("the size of a is %d\n ",sizeof(a));
  printf("the size of b is %c\n ",sizeof(b));
  printf("the size of c is %f\n ",sizeof(c));
  printf("the size of d is %lf\n ",sizeof(d));

return 0;
}